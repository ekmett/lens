0.1 [????.??.??]
----------------
* Initial release. Provides `Control.Lens.PartialIso` with the `InvPrism` and
  `PartialIso` optic types, the `invPrism` and `partialIso` constructors, the
  `failing'` fallback combinator, and the `(#?)` partial-review operator.
